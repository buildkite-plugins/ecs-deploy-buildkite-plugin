name: ECS Deploy
description: Deploy to ECS
author: https://github.com/envato
requirements:
  - aws
  - docker
configuration:
  properties:
    deploy:
      type: [ string, array ]
    validate:
      type: [ string, array ]
  oneOf:
    - required:
      - deploy
    - required:
      - validate
