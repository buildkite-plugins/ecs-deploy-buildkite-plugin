steps:
  - label: ":docker: :hammer:"
    plugins:
      docker-compose#v2.5.1:
        run: tests

  - label: ":shell: Lint"
    plugins:
      plugin-linter#v2.0.0:
        id: ecs-deploy

  - label: ":shell: Shellcheck"
    plugins:
      shellcheck#v1.0.1:
        files: hooks/**
