steps:
  - label: ":docker: :hammer:"
    plugins:
      - plugin-tester#v1.1.0: ~

  - label: ":shell: Lint"
    plugins:
      plugin-linter#v3.2.0:
        id: ecs-deploy

  - label: ":shell: Shellcheck"
    plugins:
      shellcheck#v1.3.0:
        files:
          - hooks/**
          - lib/**
